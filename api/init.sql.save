CREATE USER api_user WITH ENCRYPTED PASSWORD 'apipassword';
CREATE DATABASE api:
GRANT ALL PRIVILIGES ON DATABASE api TO api_user;
\c api;
CREATE TABLE IF NOT EXISTS lessons(id SERIAL PRIMARY KEY, name VARCHAR(255) NOT NULL);
CREATE TABLE IF NOT EXISTS themes(lesson_id INT, theme VARCHAR(255), FOREIGN KEY(lesson_id)) REFERENCES lesson(id) ON DELETE CASCADE;
